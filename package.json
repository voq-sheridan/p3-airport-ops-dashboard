{
  "name": "p3-airport-ops-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "deploy": "observable deploy",
  "clean": "rm -rf dist src/.observablehq/cache",
    "predeploy": "touch dist/.nojekyll",
    "publish-gh": "npm run build && touch dist/.nojekyll && gh-pages -d dist --dotfiles"
  },
  "dependencies": {
    "@observablehq/framework": "^1.12.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
